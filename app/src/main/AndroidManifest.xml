<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otlobna">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Views.UpdateOrderActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Views.TrackingMap"
            android:label="@string/title_activity_tracking_map" />
        <activity
            android:name=".Views.UpdateLocationMap"
            android:label="@string/title_activity_update_location_map" />
        <activity
            android:name=".Views.AddLocationOrder"
            android:label="@string/title_activity_add_location_order" />
        <activity
            android:name=".Views.Orders"
            android:label="@string/title_activity_orders"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.Other"
            android:label="@string/title_activity_other"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.Requestes"
            android:label="@string/title_activity_requestes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.Request"
            android:label="@string/title_activity_request"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Adapter.Main" />
        <activity
            android:name=".Views.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Views.Welcome" />
        <activity android:name=".Views.SingUp" />
        <activity android:name=".Views.Login" />
        <activity
            android:name=".Views.Splash"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.TestAPIinFrag" />
        <activity android:name=".Views.IntroActivity" />

        <service android:name=".Service.GPS_Service" />

        <activity android:name=".Views.ProfileActivity" />
        <activity android:name=".Views.AvailableTayarActivity" />
        <activity android:name=".Views.ContentRequestActivity" />
    </application>

</manifest>